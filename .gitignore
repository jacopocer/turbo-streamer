# Build output
.build/
*.app/
*.dSYM/

# Local ffmpeg binaries (too large / architecture-specific)
bin/

# Fonts — commercial licenses, not redistributable
# Purchase Sofia Pro from mostardesign.com and Bello Pro from underware.nl
# then place .otf files in Resources/Fonts/
Resources/Fonts/

# macOS noise
.DS_Store
*.xcuserstate

# Swift PM
.swiftpm/

# Claude Code session data / worktrees
.claude/

# MediaMTX binary for the receiver app (fetched, not tracked)
receiver/vendor/

# turbo-net helper binary (built by net/build-net.sh)
net/bin/

# deploy-apps.sh --dry-run output
.deploy-dry-run/

# NDI SDK installed on this Mac (written by receiver/ndi/ndi-sdk.sh)
receiver/ndi/.ndi-sdk-installed
